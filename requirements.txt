# ============================================================
#  BraTS / MR-RATE Diffusion Model — Requirements
#  Python 3.9.x  (tested on 3.9.13)
#
#  Install order matters — torch before monai.
#  GPU (DirectML / CUDA): see notes at the bottom.
# ============================================================

# ── Core numerics ────────────────────────────────────────────
numpy==2.0.2
scipy==1.13.1
pillow==11.3.0

# ── Medical imaging I/O ──────────────────────────────────────
nibabel==5.3.3

# ── PyTorch  (CPU build — swap for CUDA/DirectML below) ──────
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# ── MONAI generative (DiffusionModelUNet, schedulers) ────────
# monai[all] pulls einops + lpips which the UNet internals need
monai[all]>=1.3

# ── LoRA / attention utilities ────────────────────────────────
einops==0.8.1       # already pulled by monai[all], pinned for safety
lpips               # perceptual loss, pulled by monai[all]

# ── Visualisation ────────────────────────────────────────────
matplotlib==3.9.4
seaborn==0.13.2

# ── Jupyter environment ──────────────────────────────────────
jupyter==1.1.1
jupyterlab==4.4.10
notebook==7.4.7
ipywidgets==8.1.7       # interactive BraTS modality viewer
ipykernel==6.30.1

# ── Data download ────────────────────────────────────────────
huggingface-hub==0.34.4

# ── Misc utilities ───────────────────────────────────────────
tqdm==4.67.1
scikit-learn==1.6.1     # used for metrics (AUC) at evaluation stage

wandb 

# ============================================================
#  GPU NOTES
# ============================================================
#
#  Windows DirectML (AMD / Intel / any DX12 GPU):
#    pip install torch-directml==0.2.5.dev240914
#    (already installed in this env)
#    Usage in code:  device = torch_directml.device()
#
#  NVIDIA CUDA 12.x:
#    pip install torch==2.4.1+cu121 torchvision==0.19.1+cu121 \
#        --index-url https://download.pytorch.org/whl/cu121
#
#  NVIDIA CUDA 11.8:
#    pip install torch==2.4.1+cu118 torchvision==0.19.1+cu118 \
#        --index-url https://download.pytorch.org/whl/cu118
#
#  Mac MPS (Apple Silicon):
#    Standard torch==2.4.1 from PyPI — MPS is built in.
#    device = "mps" if torch.backends.mps.is_available() else "cpu"
# ============================================================
